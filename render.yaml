services:
  - type: web
    name: bandi-scanner
    runtime: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
