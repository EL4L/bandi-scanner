# INCIDENTS — Bandi Scanner

Registro incidenti tecnici: problema, impatto, causa, fix applicato.

| Data | Descrizione | Impatto | Causa | Fix |
|------|-------------|---------|-------|-----|
| 2026-05-21 | Contenuto log copiato da altro progetto (MLPG) | `PROMPT_LOG.md` / `INCIDENTS.md` non riflettevano bandi-scanner | File template errati nella cartella `logs/` | Sostituzione completa con documentazione bandi-scanner |
| 2026-05-21 | API key esposta in `.env.example` | Rischio sicurezza se committata su Git | Chiave reale inserita per errore nel template | Sostituita con `ANTHROPIC_API_KEY=` vuoto; ruotare chiave su console Anthropic se già pushata |
| 2026-05-21 | `test_phase1.py` non trova PDF | Test Fase 1 fallito con messaggio “Nessun PDF” | PDF in `data/` ma script legge solo `data/test_pdfs/` | Copia PDF in `data/test_pdfs/` (`Complesso.pdf`, `Semplice.pdf`) |
| 2026-05-21 | `streamlit` / `python` non riconosciuti in PowerShell | Impossibile avviare app da terminale | venv non attivato o PATH senza Scripts | `.\venv\Scripts\Activate.ps1` oppure `.\venv\Scripts\streamlit.exe run app.py` |
| 2026-05-21 | `Activate.ps1` bloccato | Errore ExecutionPolicy | Policy script disabilitata su Windows | `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` |
| 2026-05-21 | Typo comando (`stremalit`, `treamlit`) | Comando non trovato | Errore battitura | Usare `streamlit run app.py` |
| 2026-05-21 | `data_scadenza` vuota in UI nonostante data nel PDF | RF-002 non soddisfatto; utente vede campo null | Prompt troppo prudente (“usa \"\" se non certo”); Claude non estrae; PDF lunghi troncati | Prompt v1.1/v2.0; modulo `date_infer.py`; inferenza post-estrazione in `validate_bando()` |
| 2026-05-21 | Costo API a ogni upload PDF | ~0,05 € per caricamento | `extract_bando_data()` chiamata automaticamente dopo upload | Estrazione AI solo su pulsante dedicato in `app.py` |
| 2026-05-21 | JSON Claude non parsabile | Estrazione fallita, `InvalidJSONResponse` | Risposta con markdown o testo extra | `_clean_json_response()` rimuove fence ```json |
| 2026-05-21 | API Anthropic down / rate limit | Estrazione bando fallita dopo attesa | Errore rete o 5xx | Retry 3× con attesa 300 s (`tenacity`); log su `error_log.txt` e riga in questo file |
| 2026-05-21 | PDF illeggibile (scan senza testo) | Messaggio errore upload | Testo estratto <100 caratteri | `EmptyPDFException` + messaggio UI; mitigazione futura: OCR (Fase 5) |
| 2026-05-21 | Validatore non collegato all’app (Fase 1 iniziale) | JSON mostrato senza warning RF-007 | `validate_bando()` non importato in `app.py` | Integrazione validazione + warning “Da revisionare manualmente” |
| 2026-05-21 | Schema JSON cambiato (v2 `bando`) | Log e test vecchi con nomi campo obsoleti (`link_fonte`, `codici_ateco`) | Migrazione schema Melanie | `modules/schema.py`, prompt 2.0, validatore aggiornato |
| 2026-05-21 | Progetto spostato su disco locale | Possibile venv rotto / path errati | Spostamento cartella da sync cloud | Verificare `cd` su `C:\bandi-scanner` o percorso attuale; ritestare `venv\Scripts\python.exe` |
| 2026-05-21 | `ModuleNotFoundError: anthropic` fuori venv | Script test fallito | Python di sistema invece del venv | Usare `.\venv\Scripts\python.exe scripts\test_phase1.py` |
| 2026-05-26 | Matching restituisce 100% per tutti i clienti su bando privo di vincoli (es. `Semplice.pdf`) | Dashboard con falsi positivi; possibile invio di alert errati | Logica precedente assegnava pesi pieni per dimensione/fatturato/regioni per default quando il bando non specificava vincoli | Introdotta `_bando_has_constraints` e wrapper `bando_has_constraints`; se bando vuoto o senza vincoli ora score=0 e log diagnostico; cambiati default di `_score_dimensione` e `_score_fatturato` per non assegnare peso pieno se campo assente |
| 2026-05-26 | Discrepanze tra score salvati in DB e ricalcolo dinamico (breakdown) | Visualizzazione incoerente in dashboard, confusione operativa | Score salvati in `match_results` erano prodotti da logiche precedenti e/o breakdown costruito con dati parziali | Aggiornata UI per calcolare breakdown usando il record cliente completo dal DB; aggiunto warning e pulsante per ricalcolo matching; script `scripts/simulate_matching.py` per debug |
2026-05-31 | API provider (DeepSeek/OpenRouter) instabile e in timeout multiplo | Blocco dei test di estrazione AI in UI | Disservizio lato server OpenRouter (Connection Error) | Aggiunto mock manuale di JSON temporaneo per continuare sviluppo UI. Migrazione a provider stabile completata. |
| 2026-05-31 | Risoluzione Incidente O-01 (Troncamento PDF severo) | Perdita di data di scadenza ed esclusioni nei bandi >40 pag. | La funzione `_truncate_text` scartava i paragrafi se le keywords non combaciavano esattamente. | Implementato chunking intelligente in `extractor.py`: ampliate le keywords (`"termin"`, `"domanda"`, `"presentazione"`, `"esclusion"`). |
| 2026-06-03 | Crash del `Validator` per Schema Mismatch | Blocco app: `Tipo non valido per bando.note_esclusioni` | Il prompt è stato migliorato per restituire un JSON Strutturato (`dict`), ma `schema.py` richiedeva ancora `str`. | Aggiornato `BANDO_SCHEMA` per accettare formati flessibili: `(dict, str, type(None))`. |
| 2026-06-03 | Falsi positivi sul Matching ATECO (Risoluzione parziale O-03) | Score 100% per clienti con settori vietati | Claude impostava `ateco_aperto_a_tutti: true` in mancanza di elenchi positivi, ignorando i divieti. | Regola rigida introdotta nel prompt. Logica UI modificata per intercettare le esclusioni e stampare alert/liste puntate in dashboard. |
| 2026-06-25 | Query N+1 nel rendering della Dashboard | Una `SELECT * FROM clienti WHERE id = ?` eseguita ad ogni iterazione del loop sui match (per ogni cliente, per ogni bando), invece di un'unica query | `load_dashboard_rows()` in `modules/matcher.py` non includeva `regione`/`fatturato`/`dimensione_impresa` del cliente, costringendo `app.py` a una query aggiuntiva per riga dentro l'expander "Vedi Clienti Compatibili" | Estesa la JOIN in `load_dashboard_rows()` per includere `c.regione`, `c.fatturato`, `c.dimensione_impresa`; rimossa la query per-riga in `app.py`, ora i dati cliente sono letti direttamente dalla riga già caricata |
| 2026-06-25 | `ModuleNotFoundError: openai` all'avvio dell'app | App in crash all'apertura della pagina (`modules/extractor.py` importa `openai`) | Package `openai` non installato nel venv e assente da `requirements.txt`, nonostante l'uso nel codice | Installato `openai` nel venv (`pip install openai`) e aggiunto a `requirements.txt` |
| 2026-06-25 | Predisposizione deploy su Render | `db/bandi.db` su path hardcoded relativo al progetto, incompatibile con filesystem effimero di Render | Nessuna astrazione del path DB tramite env var; nessun Dockerfile/render.yaml nel repo | Aggiunta variabile `DB_PATH` (override in `modules/database.py`, fallback al path locale esistente); creati `Dockerfile`, `render.yaml` (Persistent Disk su `/var/data`) e `.dockerignore`. Superato dalla migrazione a Neon (riga successiva) |
| 2026-06-25 | Migrazione da SQLite a Postgres (Neon) | Su Render senza disco persistente (piano Free) i dati in SQLite venivano persi ad ogni redeploy/restart del container | Filesystem del container effimero; SQLite scrive su file locale al container | Riscritti `db/init_db.py` (schema Postgres: `SERIAL`, `TIMESTAMP DEFAULT NOW()`, `information_schema` per migrazioni) e `modules/database.py` (driver `psycopg2`, wrapper `_PGConnection` compatibile con l'API `conn.execute(...)` esistente, placeholder `?`→`%s`, `RETURNING id` al posto di `lastrowid`); adattate query in `modules/matcher.py` (`datetime('now')`→`NOW()`, `COLLATE NOCASE`→`LOWER(...)`). Richiede env var `DATABASE_URL` (connection string Neon). `render.yaml` aggiornato: piano `free`, niente Persistent Disk. Testato end-to-end in locale contro Neon: init schema, insert/select/delete cliente, query dashboard — tutti OK |
| 2026-06-25 | `ModuleNotFoundError: psycopg2` (e poi altri moduli) avviando `streamlit run app.py` dopo il merge della migrazione a Postgres | App non avviabile in locale | Le dipendenze erano installate nell'interprete Python globale (`C:\Python314\python.exe`), non nel venv del progetto (`venv\Scripts\python.exe`) effettivamente usato da `streamlit run` | Installate/reinstallate tutte le dipendenze direttamente in `venv\Scripts\python.exe -m pip install -r requirements.txt` |
| 2026-06-25 | Doppia copia del progetto sul PC: `C:\bandi-scanner` (aggiornata, Postgres) e `C:\Users\user\bandi-scanner` (vecchia, SQLite, con un proprio venv) | Confusione su quale cartella/venv venisse effettivamente eseguito, comportamento incoerente tra avvii | Vecchia clone del repository mai rimossa dopo lo spostamento del progetto su `C:\bandi-scanner` | Verificato che la vecchia copia non avesse modifiche utili non commitate (solo residui di marker di merge mai puliti in `app.py`) ed eliminata `C:\Users\user\bandi-scanner` |
| 2026-06-25 | `Fatal error in launcher` per `streamlit.exe` subito dopo l'eliminazione della vecchia cartella | App non avviabile nemmeno dal venv corretto | Il venv era stato creato quando il progetto risiedeva ancora in `C:\Users\user\bandi-scanner`: l'eseguibile `streamlit.exe` (launcher generato da pip) ha il path assoluto al `python.exe` del venv scritto al suo interno, ormai puntato a una cartella cancellata | `pip install --force-reinstall --no-deps streamlit` nel venv per rigenerare il launcher con il path corretto (`C:\bandi-scanner\venv\...`) |
| 2026-06-25 | `RuntimeError: DATABASE_URL non configurata` nonostante la variabile fosse presente in `.env` | App non si connette a Neon pur con `.env` configurato correttamente | `modules/database.py` leggeva `os.getenv("DATABASE_URL")` a livello di modulo senza mai chiamare `load_dotenv()`; in `app.py` il modulo `database` veniva importato prima di `extractor.py` (unico punto che chiamava `load_dotenv()`), quindi la variabile non era ancora caricata in memoria al momento della lettura | Aggiunta chiamata a `load_dotenv()` direttamente in `modules/database.py`, prima della lettura di `DATABASE_URL` |
| 2026-06-25 | Migrazione completa dell'interfaccia da Streamlit a FastAPI + Jinja2 (server-rendered), in preparazione al deploy su Render | Non un incidente ma una riscrittura architetturale dell'UI: `app.py` (Streamlit, rerun-based) sostituito da `main.py` (FastAPI, route esplicite, request/response stateless) | Necessità di abbandonare Streamlit come UI per il deploy su Render | Creato `main.py` con route per dashboard, estrazione bandi e gestione clienti; template Jinja2 in `templates/` (+ partial in `templates/partials/`) e CSS estratto in `static/styles.css`; pattern redirect-after-POST per le mutazioni (create/update/delete cliente, recalc match); upload PDF + estrazione AI collassati in un solo POST sincrono (fedele al flusso originale, che non usava `st.session_state`); download "scheda" ed export CSV trasformati in endpoint HTTP reali al posto dei link `data:` base64; `requirements.txt` aggiornato (rimosso `streamlit`, aggiunti `fastapi`, `uvicorn[standard]`, `jinja2`, `python-multipart`); `Dockerfile` aggiornato per lanciare `uvicorn main:app`; rimossi `app.py` e `.streamlit/config.toml`. Logica in `modules/` (database, matcher, extractor, validator, log_utils) riutilizzata senza modifiche. Verificato end-to-end in locale contro Neon (dashboard, recalc, export CSV, CRUD clienti, download scheda); non testato il flusso reale di upload PDF + chiamata LLM per mancanza di un PDF di prova in sessione |

| 2026-06-25 | Bandi duplicati nel DB (stesso titolo + ente) mostrati come card separate in Dashboard | Visualizzazione ridondante; score e match calcolati più volte sullo stesso bando | Upload ripetuto dello stesso PDF senza controllo di unicità in `save_bando_from_json` | Aggiunta `deduplica_bandi()` in `modules/database.py` (GROUP BY titolo/ente, elimina gli id inferiori e relativi `match_results`); endpoint `POST /api/bandi/deduplica` in `main.py`; Dashboard raggruppa le card per titolo unico (useMemo, mantiene id più alto), mostra badge contatore duplicati e bottone Deduplica con feedback post-operazione |
| 2026-06-25 | Migrazione UI da Jinja2/server-rendered a React SPA | Frontend completamente riscritto in React + Vite; nessun impatto sui moduli backend | Necessità di una UI più reattiva e indipendente dal template rendering | Creati `frontend/src/App.tsx` (sidebar + BrowserRouter), `components/Dashboard.tsx`, `components/Bandi.tsx`, `components/Clienti.tsx`, `components/CaricaBando.tsx`, `frontend/src/styles.css` (design system completo, sidebar #1e3a5f, badge urgenza). Backend FastAPI invariato; Vite proxya `/api` su porta 8000 |
| 2026-06-26 | Valori null mostrati come "N/D", "null", "None" in UI | Esperienza utente degradata con testo tecnico visibile | Campi mancanti non filtrati prima del render | Helper `isBlank()` centralizzato; tutti i campi opzionali (urgenza, scadenza, contributo_max, giorni_alla_scadenza) condizionalmente nascosti se blank; badge urgenza visibili solo se presente |
| 2026-06-26 | Upload duplicato dello stesso PDF ricreava un secondo bando identico nel DB | Bandi duplicati non bloccati dall'inserimento ripetuto dello stesso PDF | Nessun controllo di unicità in `save_bando_from_json` al momento del salvataggio | Aggiunta `find_duplicate_bando(titolo, ente)` in `modules/database.py` (SELECT case-insensitive con LOWER); in `main.py` endpoint `/api/estrazione` controlla duplicato prima di `save_bando_from_json` e restituisce `{"status": "duplicato", "bando_id": <id_esistente>}`; `CaricaBando.tsx` mostra banner giallo con link scarica scheda e link vai ai Bandi |
| 2026-06-26 | Separazione bandi attivi/scaduti in Bandi.tsx e Dashboard.tsx | Bandi scaduti mescolati con quelli attivi, difficile distinzione visiva | Assenza di logica di separazione per `giorni_alla_scadenza < 0` | `Bandi.tsx` riscritto con `isExpired()`, filtro rapido (Tutti/Attivi/Scaduti), sezione scaduti collassabile con toggle; `Dashboard.tsx` aggiornato con sezione "Bandi scaduti" collassabile (default nascosta) con `bando-card-expired` (opacity 0.58); CSS aggiornato con `.quick-filter`, `.scaduti-toggle`, `.section-scaduti-header/body`, `.bando-card-expired` |
| 2026-06-26 | Redesign visivo frontend (elevazione a SaaS B2B per commercialisti) | Solo presentazione/feedback UI; logica fetch, API e nomi funzioni invariati | Rifinitura estetica dell'MVP con gap rispetto al brief: feedback solo inline (no toast), score circle piatto, niente rail urgenza sulle card, tabelle senza zebra/header sticky, voce sidebar attiva senza accent | `styles.css`: `.score-circle` ora anello `conic-gradient` proporzionale via `--score`; rail sinistro colorato per urgenza su `.bando-card` (`--alta/--media/--bassa/--scaduto`); tabelle con righe alternate + `th` sticky; voce sidebar attiva con accent `#3b82f6` + gradiente navy; hover KPI; ombra `.btn-primary`; `prefers-reduced-motion`. Nuovo sistema toast (`src/toast.ts` + `components/ToastHost.tsx`, montato in `App.tsx`) cablato su deduplica/recalc, CRUD clienti e upload bando — additivo, nessuna modifica alla logica esistente. Build `tsc -b && vite build` OK |

| 2026-07-02 | `load_dashboard_rows()` non includeva `numero_dipendenti` del cliente nella JOIN | `check_ammissibilita()` non riceveva il campo → verifica anzianità sempre "non verificabile" anche con dato presente | Colonna assente dalla SELECT in `modules/matcher.py` + campo mancante in `cliente_row` in `main.py` `_dashboard_payload()` | Aggiunto `c.numero_dipendenti AS cliente_numero_dipendenti` alla JOIN in `load_dashboard_rows()`; aggiunto `"numero_dipendenti": m.get("cliente_numero_dipendenti")` in `cliente_row` |
| 2026-07-02 | `genera_scheda()` non mostrava i requisiti di accesso (anzianità, forma giuridica, spesa min/max) nella scheda bando | Dati estratti dal LLM ma non presentati all'utente nella scheda di sintesi | Sezione mancante in `modules/matcher.py` → `genera_scheda()` | Aggiunta sezione `## Requisiti di accesso` con 5 campi condizionali (spesa_min, spesa_max, mesi_min, mesi_max, forme_giuridiche); visibile solo se almeno un campo è presente e non null |
| 2026-07-02 | `Dockerfile`: directory `logs/` non creata nel container | `log_utils.py` crashava al primo log in produzione su Render; `$PORT` senza fallback → possibile errore avvio se env var non iniettata | Dockerfile non includeva `RUN mkdir -p /app/logs`; CMD usava `$PORT` senza default | Aggiunto `RUN mkdir -p /app/logs`; CMD cambiato in `--port ${PORT:-8000}` |
| 2026-07-02 | `RETRY_WAIT_SECONDS = 300` hardcoded in `modules/extractor.py` | Se DeepSeek fallisce, l'endpoint `/api/estrazione` blocca il thread per 15 minuti (3 × 300s) senza feedback all'utente | Valore fisso non configurabile | Reso configurabile via env var `LLM_RETRY_WAIT_SECONDS`; default abbassato a 60s |
| 2026-07-02 | `Dashboard.tsx` mostrava al massimo 5 clienti compatibili per bando (`.slice(0, 5)`) | Match oltre il quinto invisibili — utenti con più di 5 clienti perdevano abbinamenti | Limite hardcoded nel render della lista match | Rimosso `.slice(0, 5)`: ora tutti i match sono visibili |
| 2026-07-02 | `CaricaBando.tsx` esponeva un blocco `<details>Raw JSON estratti (debug)</details>` visibile agli utenti finali | Unprofessionale in contesto B2B; espone struttura interna del JSON estratto | Blocco debug lasciato nel codice senza flag di produzione | Rimosso completamente il blocco |
| 2026-07-02 | `CaricaBando.tsx` non validava la dimensione del file prima dell'upload | File >10MB veniva inviato al server, occupando il thread per 30+ secondi prima di un eventuale errore | Nessun check `file.size` in `handleFile()` | Aggiunto check client-side: se `file.size > 10_000_000` mostra toast + banner rosso con dimensione effettiva e blocca l'upload |
| 2026-07-02 | `POST /api/estrazione` non validava la dimensione del file lato server | Un PDF >10MB poteva bloccare il thread per 30+ secondi prima di fallire | Nessun check su `len(file_bytes)` prima del salvataggio su disco e della chiamata LLM | Aggiunto check `len(file_bytes) > 10_000_000` con risposta 400 immediata includente dimensione effettiva del file |
| 2026-07-02 | `modules/schema.py`: campo `spesa_massima_ammissibile` assente da `BANDO_SCHEMA` | `genera_scheda()` leggeva il campo ma il validatore e il LLM non lo conoscevano → sempre `None` | Campo aggiunto a `genera_scheda()` senza aggiungerlo a schema e prompt | Aggiunto `"spesa_massima_ammissibile": (int, float, type(None))` in `BANDO_SCHEMA`; aggiunto al prompt v3.1 con istruzioni esplicite e esempio JSON |
| 2026-07-02 | `Clienti.tsx`: form di creazione/modifica senza validazione client-side su P.IVA e ATECO | Errori di formato arrivavano solo dal server dopo una chiamata API | Nessun check regex nel `handleSubmit` | Aggiunta validazione: P.IVA `/^\d{11}$/`, ATECO `/^\d{2}\.\d{2}(\.\d{2})?$/`; errori mostrati nel banner `formErrors`, submit bloccato |
| 2026-07-02 | `modules/extractor.py`: se DeepSeek fallisce, nessun fallback immediato — solo retry con attesa | Durante la demo un down di DeepSeek avrebbe causato 15 minuti di blocco totale | Retry hardcoded su stesso modello, nessun fallback su modello alternativo | Aggiunto `LLM_FALLBACK_MODEL` (default `claude-haiku-4-5-20251001`); se il modello primario fallisce → tentativo immediato su fallback; entrambi configurabili via env var |
| 2026-07-02 | `modules/matcher.py`: `calculate_score()` e `get_score_breakdown()` restituivano 0 per bandi aperti a tutti (`ateco_aperto_a_tutti=True`) | Bandi genuinamente aperti a tutti i settori venivano ordinati in fondo al matching come se non avessero match | `bando_has_constraints()` non distingueva tra bandi vuoti/ambigui e bandi esplicitamente aperti a tutti | Fix: `score=0` solo se `bando_has_constraints=False` AND `ateco_aperto_a_tutti=False` (bando ambiguo); se `ateco_aperto_a_tutti=True` → scoring normale |
| 2026-07-02 | `requirements.txt` senza versioni pinnate | Upgrade automatico dipendenze in deploy poteva rompere il build | Nessuna versione specificata in `requirements.txt` | Pinning a versioni attuali del venv (pip freeze): fastapi==0.138.1, uvicorn==0.47.0, psycopg2-binary==2.9.12, etc. |
| 2026-07-02 | Codice `renderMarkdown` e modal scheda duplicati in `Dashboard.tsx` e `Bandi.tsx` (~80 righe per componente) | Bug fix o modifiche allo stile della scheda richiedevano modifiche in 2 posti | Pattern modal non estratto in componente condiviso | Creato `frontend/src/components/ModalScheda.tsx` con `ModalScheda` + `renderMarkdown` esportati; entrambi i componenti ora importano da lì; `tsc --noEmit`: 0 errori |
| 2026-07-02 | Aggiunto `POST /api/bandi/{id}/rigenera-scheda` — dopo modifica al prompt v3.1 le schede esistenti in `scheda_cached` non includevano la nuova sezione "Requisiti di accesso" e `spesa_massima_ammissibile` | Schede vecchie aggiornate senza re-upload PDF | Nessun endpoint per rigeneare `scheda_cached` senza ricaricare il PDF | Endpoint `POST /api/bandi/{id}/rigenera-scheda`: legge `json_completo`, chiama `genera_scheda()`, aggiorna `scheda_cached`; risposta: `{bando_id, scheda}` |

---

## Incidenti noti ancora aperti

| ID | Problema | Workaround |
|----|----------|------------|
| O-02 | **Semplice.pdf**: scadenze solo relative | `data_scadenza` = `null` corretto; revisione manuale |
| O-03 | Integrazione logica DB su Esclusioni ATECO | Gestione visiva (UI) implementata. Necessario completare task 4.1 per l'abbattimento automatico dello score in presenza di `ateco_escluse` strutturate. |
 |



---

## File di log correlati

- `error_log.txt` — errori runtime append-only (API, PDF, salvataggio)
- `logs/PROMPT_LOG.md` — versioni prompt e risultati test PDF

---
